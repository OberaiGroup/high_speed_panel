On earth, use:

gmsh_4_2 panel.geo -3 -order 2 -save

to use gmsh v4.2 to make the panel mesh 
with 3D elements, 2nd order, and save to 
file. 

Default output is .msh. Change to vtk
for paraview with:

gmsh_4_2 panel.geo -3 -order 2 -save -format vtk
